using System;

public class SvtVoiceType
{
    public enum Type
    {
        BATTLE = 6,
        EVENT_JOIN = 4,
        EVENT_REWARD = 5,
        FIRST_GET = 3,
        GROETH = 2,
        HOME = 1,
        SUM = 8,
        TREASURE_DEVICE = 7
    }
}

